# Engine C++-bound Lua API — corpus inventory

> Last updated: 2026-05-03 — this is the index doc; the canonical
> auto-generated reference is at `build/wire/cpp_bindings.md`
> (regenerated by `make extract-cpp-bindings`).

## What this captures

Per `docs/world_master_decomp.md`, the engine declares its
C++-implemented Lua-callable methods using the
`_<method>_cpp` / `_<method>_inl` pair convention in `_u`-suffix
files (encoded as `_p` in the ciphered tree). The `_inl` body is a
2-line stub returning the literal strings `"self"` and
`"_<method>_cpp"`; the engine reads those literals at call time and
dispatches to the matching C++ implementation (per the
`LuaActorImpl` 90-slot binding from Phase 6 item #6).

`tools/extract_cpp_bindings.py` walks every `*_p.luac` file under
`build/lpb/` and scans each file's string table for `_<name>_inl`
patterns. Each match is one C++-bound method on the parent class.

## How to regenerate

```bash
make decode-lpb              # populates build/lpb/*.luac (one-time)
make extract-cpp-bindings    # → build/wire/cpp_bindings.{json,md}
```

`make lpb-corpus` runs all three steps (decode + decompile +
extract) in sequence.

## Headline numbers (corpus snapshot 2026-05-03)

- **26 `_u` files scanned** — most class-registration roots have one
- **19 classes have C++ bindings** (others are pure-Lua with no engine glue)
- **429 total method declarations** across all classes
- **419 distinct method names** — nearly all class-unique; only 10 methods are shared across multiple classes

## Top 10 classes by C++-bound API surface

| Rank | Class | Methods | Comment |
|---:|---|---:|---|
| 1 | **PlayerBaseClass** | 94 | Player-specific actions (talk/emote/notice/push lifecycle, GM checks, content-command flags, party operations) |
| 2 | **CharaBaseClass** | 76 | Shared chara API (stats, inventory, group, status effects) |
| 3 | **desktopWidget** | 43 | UI manager (widget container ops, target cursor, user config, user macros, log pool) |
| 4 | **math** | 32 | Lua's math library re-shimmed as engine bindings (`_abs`, `_acos`, `_atan`, etc.) — likely so the engine can deterministically replay math ops |
| 5 | **WidgetBaseClass** | 24 | Widget lifecycle |
| 6 | **NpcBaseClass** | 23 | NPC-specific (talk/push/emote dispatch + parts model) |
| 7 | **WorldMaster** | 23 | World-event router (time, calendar, tutorial controls, chocobo, logging) |
| 8 | **ItemBaseClass** | 19 | Item operations |
| 9 | **Global** | 15 | Global helpers |
| 10 | **GroupBaseClass** | 15 | Party/group operations |

Plus: string (14), ActorBaseClass (10), SpreadSheet (10), AreaBaseClass (9),
debug (7), DirectorBaseClass (5), table (5), CutScene (4),
PrivateAreaBaseClass (1).

## Cross-class shared methods (likely inherited)

Almost all 419 methods are class-unique. The 10 that recur:

| Method | Declared in N classes |
|---|---:|
| `_updateWork` | 4 |
| `_setFilename` | 3 |
| `_getDisplayName` | 2 |
| `_getExtendedTemporaryGroupByDisplayName` | 2 |
| `_getGroupByDisplayName` | 2 |
| `_breakNotice` | 2 |
| `_getProperty` | 2 |

The fact that 419 of 429 methods are class-unique means the engine
has a **clean per-class API surface separation** — almost no
cross-class binding redundancy.

## What this enables for garlemald

Garlemald's Rust-side Lua bindings (per
`map-server/src/lua/userdata.rs` and friends) need to expose
**every method in this inventory** for the corresponding class.
Coverage gaps will manifest as `attempt to call a nil value (method
'_xxx_cpp')` errors at script-execution time.

Per `docs/garlemald_validation.md`, garlemald's `userdata.rs`
exposes ~50 player methods. The decomp inventory shows
**PlayerBaseClass needs 94 C++-bound methods** + the inherited
CharaBaseClass methods (76) on top of that — so the player binding
needs ~170 distinct method signatures to reach full coverage. The
gap (~120 methods) is what garlemald will surface as scripts touch
deeper player-state queries.

## Most actionable findings

### Top "missing" PlayerBase methods garlemald likely needs

(Sample from the full list; complete reference at
`build/wire/cpp_bindings.md`.)

- **Talk lifecycle**: `_executeTalk`, `_canExecuteTalk`,
  `_cancelTalk`, `_breakTalk`
- **Emote lifecycle**: `_executeEmote`, `_canExecuteEmote`,
  `_cancelEmote`, `_breakEmote`
- **Notice lifecycle**: `_cancelNotice`, `_breakNotice`
- **Push lifecycle**: `_cancelPush`, `_breakPush`
- **Server callbacks**: `_callServerOnTalk`, `_callServerOnEmote`,
  `_callServerOnPush`, `_callServerOnCommand`
- **Event mode**: `_isEventPlaying`
- **GM**: `_getGMRank`

These mirror the `onTalk`/`onPush`/`onEmote` server-side dispatch
garlemald already drives — the C++-bound methods are the
client-side terminations of those flows.

### desktopWidget surface = the UI control plane

The 43 desktopWidget methods cover **widget container ops, target
cursor control, user config persistence, user macro CRUD, and
target-tutorial flow control**. This is the canonical UI-control
API. Garlemald-side, the relevant packets are:
- Widget-create/destroy → mapped to `_createWidgetInWidgetContainer`
  / `_deleteCreatingWidgetInWidgetContainer`
- Target update → `_setTargetCharacter` /
  `_setTargetCharacterByDisplayName`
- User config / macro persistence → garlemald serves these from
  the per-player save state

### `math` re-shimming is intentional

Lua's standard `math` library (32 functions: `_abs`, `_acos`,
`_asin`, `_atan`, `_atan2`, `_byte`, etc.) is **re-shimmed as
engine bindings** rather than just exposed via Lua's stdlib. This
is likely so the engine can:
1. Deterministically replay math ops (for client/server consistency)
2. Substitute fixed-point or platform-stable implementations where
   Lua's float-based defaults would diverge across hardware

Garlemald's mlua-based binding gets Lua's stdlib `math` for free —
no action needed unless quest scripts depend on FFXIV-specific
math behaviour we haven't observed yet.

## Cross-references

- `build/wire/cpp_bindings.md` — auto-generated full inventory
  (per-class detailed listing of all 429 methods)
- `build/wire/cpp_bindings.json` — machine-readable equivalent
- `tools/extract_cpp_bindings.py` — the tool
- `docs/world_master_decomp.md` — the original `_cpp/_inl` discovery
- `docs/scenario_monster_decomp.md` — Chara/Npc/Player base hierarchy
- `docs/director_quest_decomp.md` — Director hierarchy decomp
- `docs/lpb_corpus_survey.md` — corpus-wide patterns
- `docs/lpb_format.md` — wrapper format + filename cipher
- `docs/lua_actor_impl.md` — Phase 6 item #6 (the binary side of
  the C++ binding — the LuaActorImpl 90-slot vtable that backs
  these Lua-callable methods)
- garlemald-server's `map-server/src/lua/userdata.rs` — the
  Rust-side implementations garlemald must provide for each
  binding listed here
