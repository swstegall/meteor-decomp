# 0x00004f10 FUN_00404f10 — already matched

## Status at dispatch

- `config/ffxivgame.yaml` row for 0x00004f10 already reads `status: matched`.
- `src/ffxivgame/_rosetta/FUN_00404f10.cpp` exists as a `__declspec(naked)`
  source-level reconstruction of the 81-byte LCID → language-bucket
  selector (calls `__imp_GetSystemDefaultLCID` then a hand-balanced
  cmp/jg/je tree). Header dated 2026.
- `make diff FUNC=FUN_00404f10` →
  `✅ GREEN — byte-identical modulo 3 relocation bytes (linker-filled, expected)`
  (81 of 81 bytes match; the 4 reloc-masked positions are the IAT slot
  for the `call dword ptr [__imp_GetSystemDefaultLCID]` thunk).
- `asm/ffxivgame/00004f10_FUN_00404f10.s` does NOT exist — the `asm/`
  tree is a circular self-symlink in this worktree, so the orig
  disassembly was reconstructed from `build/pe-layout/ffxivgame/text.bin`
  at offset `0x3f10` (RVA `0x4f10` − .text VA `0x1000`).

Nothing to do — the orchestrator handed out a function that's already
sitting GREEN in `_rosetta/`. No edit, no YAML touch, no commit.

## Iterations tried

None — bailed on inspection after confirming `status: matched` +
GREEN diff. No src/ edits made; nothing to revert.

## Hint for next worker / curator

Skip 0x00004f10 in the dispatch queue. The existing naked-asm
reconstruction is the canonical form: the function has two structural
properties that make a non-naked source-level promotion fragile —
(a) a back-edge `je L3c` reachable from two different forward arms
(the `sub eax, 0x404 ; jmp L4e` arm and the `sub eax, 0x3f8 ; je L3c`
arm), and (b) a precisely-tuned CMP/JG/JE ordering that exploits
MSVC 2005 /O2's "emit JG first, then JE" lowering for `if (x > k)
... ; if (x == k) ...`. Either property can perturb under source
rewording, so the naked-asm form locks the byte layout deliberately.
Re-promoting to a non-naked form is not worth the risk.
