# 0x00001750 FUN_00401750 — already matched

## Status at dispatch

- `config/ffxivgame.yaml` row for 0x00001750 already reads `status: matched`.
- `src/ffxivgame/_rosetta/FUN_00401750.cpp` exists as a `__declspec(naked)`
  byte-passthrough of the orig 182-byte slice (header dated 2026).
- `make diff FUNC=FUN_00401750` → `✅ GREEN — byte-identical (182 of 182 bytes match)`.
- `asm/ffxivgame/00001750_FUN_00401750.s` does NOT exist; the `asm/`
  tree is empty in this worktree.

Nothing to do — the orchestrator handed out a function that's already
sitting GREEN in `_rosetta/`. No edit, no YAML touch, no commit.

## Why a source-level promotion is still blocked

The rosetta header lists the upstream prerequisites:

- 4 sub-object ctors not yet decompiled — `0x4b3b50`, `0x4b8640`,
  `0x445cf0`, `0x44c890`.
- SEH handler-table absolute address `0xe5438c`.
- Vtable absolute address `0xf54a24`.
- `__security_cookie` global at `0x012ea8b0`.

All four resolve only under a full-binary re-link at orig load
address; a standalone hand-written .cpp can't reproduce the call /
absolute-address bytes. Until the four ctor callees land, the
naked-asm byte-blob is the byte-correct match.

## Iterations tried

None — bailed on inspection after confirming `status: matched` +
GREEN diff. No src/ edits made; nothing to revert.

## Hint for next worker / curator

Skip 0x00001750 in the dispatch queue. If you want to promote it to a
real source-level reconstruction, do the four ctor callees first
(0x4b3b50, 0x4b8640, 0x445cf0, 0x44c890), then revisit.
