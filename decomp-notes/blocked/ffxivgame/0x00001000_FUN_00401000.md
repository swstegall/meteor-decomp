# BLOCKED: ffxivgame 0x00001000 FUN_00401000

## Status

Cannot start. Phase-1 infrastructure for `ffxivgame.exe` is not in
place in this worktree. Every input listed in the worker prompt is
missing:

- `asm/ffxivgame/00001000_FUN_00401000.s` — missing; no
  `asm/ffxivgame/` directory exists at all (only `asm/ffxivlogin/`).
- `build/ghidra-decomp/ffxivgame/00001000_FUN_00401000.c` — missing;
  no `build/ghidra-decomp/` directory exists.
- `config/ffxivgame.yaml` — missing (only `config/ffxivgame.yaml.example`).
- `config/ffxivgame.symbols.json` — missing. `make diff FUNC=…`
  errors out with exactly: "config/ffxivgame.symbols.json missing —
  run `make split BINARY=ffxivgame.exe`".
- `config/ffxivgame.rtti.json` — missing.
- `decomp-notes/idioms/ffxivgame.md` — missing (no `decomp-notes/`
  tree existed before this post-mortem).

`make split BINARY=ffxivgame.exe` was attempted and fails at the
first stage:

```
python3 tools/import_to_ghidra.py ffxivgame.exe
error: not a Ghidra install:
  /opt/homebrew/Cellar/ghidra/12.0.4/libexec  (no support/launch.sh)
```

The Homebrew `ghidra` formula on this host doesn't ship the headless
runner the decomp pipeline expects, so neither the per-function asm
dumps nor the per-function pseudo-C nor the symbols/RTTI JSON can be
regenerated from this worktree.

## What the function looks like (decoded by hand from the PE)

Read 29 bytes from `orig/ffxivgame.exe` at file offset 0x1000 (=.text
RVA 0x1000):

```
8b 44 24 08    MOV  EAX, [ESP+0x8]   ; arg2
8b 4c 24 04    MOV  ECX, [ESP+0x4]   ; arg1
50             PUSH EAX               ; arg2
51             PUSH ECX               ; arg1
e8 21 f2 03 00 CALL rel32 → 0x00440230
8b 54 24 14    MOV  EDX, [ESP+0x14]  ; arg3 (8 pushed + 4 ret + 8 args)
52             PUSH EDX               ; arg3
e8 07 07 04 00 CALL rel32 → 0x00441720
83 c4 0c       ADD  ESP, 0xc          ; caller-cleanup 3 args
c3             RET
```

cdecl, 3 args, no return value used. Structurally:

```c
void FUN_00401000(int a, int b, int c) {
    FUN_00440230(a, b);
    FUN_00441720(c);
}
```

Both callee targets — `FUN_00440230` and `FUN_00441720` — are not
yet present anywhere in `src/ffxivgame/`. Even if I were to write
a plausible `.cpp`, there's no diff infrastructure to verify it
against, and the relocs would point at extern symbols that don't
yet exist in the link.

## Iterations tried

1. Looked for `asm/ffxivgame/00001000_FUN_00401000.s` — missing.
2. Looked for `config/ffxivgame.{yaml,symbols.json,rtti.json}` — missing.
3. Ran `make diff FUNC=FUN_00401000 BINARY=ffxivgame.exe` — errors
   out: "config/ffxivgame.symbols.json missing".
4. Ran `make split BINARY=ffxivgame.exe` — errors out:
   "not a Ghidra install: …/libexec  (no support/launch.sh)".
5. Hand-decoded the 29 raw bytes from `orig/ffxivgame.exe` to confirm
   the function's shape (see above).
6. Grepped `src/ffxivgame/_rosetta/` and the whole tree for any
   reference to `FUN_00440230` / `FUN_00441720` — zero hits.

## Best guess at the root cause

The worktree's Phase-1 split for `ffxivgame.exe` has never been run
(or was never committed). Every per-function input is generated from
that step. Without it, no `ffxivgame` function can be matched in this
worktree, regardless of which RVA the orchestrator hands out.

## Hint for the next worker / curator

Don't try this function until Phase-1 is bootstrapped. Required
unblock steps, roughly in order:

1. Install (or point `GHIDRA_INSTALL_DIR` at) a Ghidra install whose
   `support/launch.sh` exists. The Homebrew `ghidra` formula's
   `libexec` only has the GUI launchers; the project needs the
   "Ghidra public release" zip, not the formula's `libexec`.
2. `make split BINARY=ffxivgame.exe` to generate
   `asm/ffxivgame/*.s`, `build/ghidra-decomp/ffxivgame/*.c`,
   `config/ffxivgame.{yaml,symbols.json,rtti.json}`.
3. Match (or at least stub) the two callees first —
   `FUN_00440230` (2-arg cdecl) and `FUN_00441720` (1-arg cdecl) —
   so this function has something to call. Without those, even a
   correctly-shaped match here can't be linked or diffed.
4. Then re-issue this RVA to a worker.

The function itself is trivial once those four things exist: it's a
straight `void FUN_00401000(int, int, int) { FUN_00440230(a, b);
FUN_00441720(c); }` written as a normal cdecl free function with no
inline asm needed.

## Re-attempt 2026-05-18

Re-issued to a second worker. State unchanged from the original
post-mortem:

- `config/ffxivgame.symbols.json` still missing.
- `asm/ffxivgame/` directory still does not exist.
- `make diff FUNC=FUN_00401000 BINARY=ffxivgame.exe` still errors out
  with `config/ffxivgame.symbols.json missing — run \`make split
  BINARY=ffxivgame.exe\``.

No new work attempted; bailing immediately to avoid wasting iterations
on infrastructure that has to land before any ffxivgame RVA is matchable.
The orchestrator should stop handing out ffxivgame RVAs until the
Phase-1 split is committed.

## Re-attempt 2026-05-18 (third worker)

Re-issued again. State STILL unchanged:

- `config/ffxivgame.symbols.json` — still missing.
- `asm/ffxivgame/` — still does not exist.
- `build/ghidra-decomp/` — still does not exist (no `ffxivgame/` subdir).
- `config/ffxivgame.{yaml,rtti.json}` — still missing.
- `decomp-notes/idioms/ffxivgame.md` — still missing.
- `make diff FUNC=FUN_00401000 BINARY=ffxivgame.exe` — same error:
  `config/ffxivgame.symbols.json missing — run \`make split
  BINARY=ffxivgame.exe\``.

No new attempt made. Bailing immediately. The orchestrator should not
hand out any `ffxivgame` RVA until Phase-1 split lands in the shared
`.git/` — re-handing this one out a third time just consumes a worker
slot without making progress. See "Hint for the next worker / curator"
above for the unblock steps.

## Re-attempt 2026-05-18 (fourth worker)

Re-issued a fourth time. State STILL unchanged — verified each input:

- `config/ffxivgame.symbols.json` — missing.
- `asm/ffxivgame/` — missing (only `asm/ffxivlogin/` exists).
- `build/ghidra-decomp/` — directory does not exist.
- `config/ffxivgame.yaml` — missing (only `.yaml.example`).
- `config/ffxivgame.rtti.json` — missing.
- `decomp-notes/idioms/ffxivgame.md` — missing.
- `make diff FUNC=FUN_00401000 BINARY=ffxivgame.exe` — same error:
  `config/ffxivgame.symbols.json missing — run \`make split
  BINARY=ffxivgame.exe\``.

No new attempt. Bailing immediately. The orchestrator is wasting
worker slots re-handing this RVA out without ever running Phase-1
bootstrap. Recommend: take ALL `ffxivgame` RVAs off the queue until
the unblock steps in "Hint for the next worker / curator" above are
done.

## Re-attempt 2026-05-18 (fifth worker)

Re-issued a fifth time. State STILL unchanged — re-verified each input
on entry:

- `config/ffxivgame.symbols.json` — missing.
- `asm/ffxivgame/` — missing (only `asm/ffxivlogin/` exists; 2270
  `.s` files for ffxivlogin, zero for ffxivgame).
- `build/ghidra-decomp/` — directory does not exist; only
  `build/{obj,logs,pe-layout}/` are present. `build/logs/ffxivgame.import.log`
  still contains the original Ghidra error: "not a Ghidra install:
  /opt/homebrew/Cellar/ghidra/12.0.4/libexec  (no support/launch.sh)".
- `config/ffxivgame.yaml` — missing (still only `.yaml.example`).
- `config/ffxivgame.rtti.json` — missing.
- `decomp-notes/idioms/ffxivgame.md` — missing (`decomp-notes/idioms/`
  directory itself does not exist).
- `src/ffxivgame/_rosetta/` — directory exists but unrelated to this
  function's prerequisites.
- `make diff FUNC=FUN_00401000 BINARY=ffxivgame.exe` — same error,
  verbatim:
  `error: /Users/swstegall/Documents/Programming/server-workspace/meteor-decomp/.worktrees/agent-1/config/ffxivgame.symbols.json missing — run \`make split BINARY=ffxivgame.exe\`` (exit 3).

No new attempt. Bailing immediately. The infrastructure problem is on
the curator's side (Ghidra install), not anything a worker can fix from
inside the worktree. Five wasted worker slots so far on this RVA —
strongly recommend the orchestrator either (a) pause all ffxivgame RVAs
until Phase-1 lands, or (b) at minimum stop re-issuing THIS RVA, since
the post-mortem above already records every attempt verbatim and there
is nothing new for a sixth worker to add.
