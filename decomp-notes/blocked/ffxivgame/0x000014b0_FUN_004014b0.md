# BLOCKED: ffxivgame 0x000014b0 FUN_004014b0

## Status

Cannot start. Same Phase-1 infrastructure problem that's been
documented (and re-documented five times) in
`decomp-notes/blocked/ffxivgame/0x00001000_FUN_00401000.md`. None of
the per-binary inputs for `ffxivgame.exe` exist in this worktree.

Verified on entry:

- `asm/ffxivgame/000014b0_FUN_004014b0.s` — missing. Only
  `asm/ffxivlogin/` exists; `asm/ffxivlogin/000014b0_FUN_004014b0.s`
  is present but it's a DIFFERENT function (27-byte tail-jump
  wrapper for ffxivlogin, not the 307-byte ffxivgame function this
  RVA refers to).
- `build/ghidra-decomp/ffxivgame/000014b0_FUN_004014b0.c` — missing.
  `build/ghidra-decomp/` directory does not exist at all.
- `config/ffxivgame.yaml` — missing (only `ffxivgame.yaml.example`,
  `ffxivgame.gam_types_rtti.json`, `ffxivgame.size_overrides.json`).
- `config/ffxivgame.symbols.json` — missing.
- `config/ffxivgame.rtti.json` — missing.
- `decomp-notes/idioms/ffxivgame.md` — missing.
- `make diff FUNC=FUN_004014b0 BINARY=ffxivgame.exe` — same error as
  every previous attempt:
  `error: …/config/ffxivgame.symbols.json missing — run \`make split
  BINARY=ffxivgame.exe\`` (exit 3).
- `make split BINARY=ffxivgame.exe` — still fails at the Ghidra step:
  `not a Ghidra install: /opt/homebrew/Cellar/ghidra/12.0.4/libexec
  (no support/launch.sh)` per `build/logs/ffxivgame.import.log`.

## What the function looks like (hand-decoded from the PE)

The orchestrator's task header says size 0x133 (307 bytes) for
`ffxivgame.exe` at RVA 0x14b0. Without a Ghidra-generated asm dump I
can't responsibly disassemble 307 bytes by hand and translate to
matching MSVC 2005 C++, so I did not attempt a write.

(Note: the unrelated 27-byte function at
`asm/ffxivlogin/000014b0_FUN_004014b0.s` is just a tail-jump wrapper
that doubles two of its four args before jumping to `0x00426ba2`. It
is NOT the same function as the ffxivgame target and would not be a
valid match.)

## Iterations tried

1. Verified `asm/ffxivgame/` does not exist.
2. Verified `config/ffxivgame.symbols.json` and `…yaml` / `…rtti.json`
   do not exist.
3. Ran `make diff FUNC=FUN_004014b0 BINARY=ffxivgame.exe` — same
   "symbols.json missing" error as the five previous workers.
4. Re-read `decomp-notes/blocked/ffxivgame/0x00001000_FUN_00401000.md`
   — root cause and unblock steps are identical; nothing new for a
   sixth worker (on a different RVA) to add beyond confirming that
   the curator-side block is still in place.

## Best guess at the root cause

Unchanged from the original post-mortem: Phase-1 split for
`ffxivgame.exe` has never run successfully in this worktree because
the Homebrew `ghidra` formula doesn't ship `support/launch.sh`. Every
per-function input the worker prompt promises is generated from that
step.

## Hint for the next worker / curator

Don't issue *any* `ffxivgame` RVA until Phase-1 is bootstrapped — see
the "Hint for the next worker / curator" section in
`decomp-notes/blocked/ffxivgame/0x00001000_FUN_00401000.md` for the
ordered unblock steps (Ghidra install → `make split` →
match callees first → re-issue). Six wasted worker slots so far across
two RVAs; this RVA's prereqs are identical to 0x1000's, so any fix that
unblocks one unblocks both.

## Re-attempt 2026-05-18 (second worker on this RVA)

Re-issued. State STILL unchanged — verified each input on entry:

- `asm/ffxivgame/` — missing (only `asm/ffxivlogin/` exists).
- `asm/ffxivlogin/000014b0_FUN_004014b0.s` — present, but it's the
  unrelated 27-byte tail-jump wrapper noted in the original
  post-mortem above (4-arg cdecl, doubles two of its args, then
  `JMP 0x00426ba2` into `_memcpy_s`; structurally a `wmemcpy_s`
  wrapper for the ffxivlogin binary — NOT the 307-byte ffxivgame
  function this task is asking about).
- `build/ghidra-decomp/ffxivgame/000014b0_FUN_004014b0.c` — missing.
- `config/ffxivgame.{yaml,symbols.json,rtti.json}` — all still missing
  (only `.yaml.example`, `.gam_types_rtti.json`, `.size_overrides.json`).
- `decomp-notes/idioms/ffxivgame.md` — missing.
- `make diff FUNC=FUN_004014b0 BINARY=ffxivgame.exe` — same error,
  verbatim: `error: …/config/ffxivgame.symbols.json missing — run
  \`make split BINARY=ffxivgame.exe\`` (exit 3).

No new attempt. Bailing immediately. Seven wasted worker slots now
across the two ffxivgame RVAs (five on 0x1000, two on 0x14b0). The
fix is curator-side (Ghidra install with `support/launch.sh` so
`make split BINARY=ffxivgame.exe` succeeds); no worker can make
progress on any ffxivgame RVA from inside the worktree until that
lands. Recommend the orchestrator dequeue all `ffxivgame` RVAs until
Phase-1 ships.
