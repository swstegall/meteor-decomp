# FUN_00414d40 — `__thiscall` ctor: installs `DefaultMemoryAllocator` handle

**Discovered**: 2026-05-24 by agent-2 while matching FUN_00414d40 (RVA 0x00014d40, 30 B).

This is a small `__thiscall` constructor on an unknown owner class that:

1. Calls a 3-byte empty base init (FUN_00414de0, `MOV EAX,ECX / RET` — the
   canonical MSVC inline form for an empty base ctor mem-init step).
2. Zeroes three consecutive DWORDs at `this+0x8`, `this+0xc`, `this+0x10`.
3. Installs an absolute pointer `0x01265f44` (a global object instance in
   the writable `.data` section) into `this+0x4`.
4. Returns `this` in EAX.

## Identified global object at VA 0x01265f44

The 16 bytes at VA `0x01265f44` (RVA `0xe65f44`, `.data` section) are a
global object instance whose first DWORD is `0x00f570b4` — the VA of
`SQEX::CDev::Engine::Phieg::Base::Memory::DefaultMemoryAllocator::vftable`
(slot count 4, per `config/ffxivgame.rtti.json` at `rva_hex: 0xb570b4`).

So the field at `this+0x4` is best read as a pointer to the singleton
`SQEX::CDev::Engine::Phieg::Base::Memory::DefaultMemoryAllocator` — an
allocator handle that the owning class delegates to.

## Inferred owner-class layout (offsets touched by the ctor)

```
[this + 0x00]  (untouched by this ctor — likely vftable, written by the
                derived ctor that calls this one, or shared with the
                base subobject FUN_00414de0 manages)
[this + 0x04]  DefaultMemoryAllocator *  ; = (DefaultMemoryAllocator *)0x01265f44
[this + 0x08]  0  (DWORD)
[this + 0x0c]  0  (DWORD)
[this + 0x10]  0  (DWORD)
[this + 0x14]  ... (end of touched range)
```

Minimum instance size: 0x14 bytes for the fields the ctor writes; the
derived ctor that calls this one will write `+0x00` (its own primary
vftable) and may extend the layout further.

## Identifying signature

The 30-byte body is a unique fingerprint:

```
56                   PUSH ESI
8b f1                MOV  ESI, ECX
e8 ?? ?? ?? ??       CALL FUN_00414de0     ; rel32
33 c0                XOR  EAX, EAX
89 46 08             MOV  [ESI+0x8],  EAX
89 46 0c             MOV  [ESI+0xc],  EAX
89 46 10             MOV  [ESI+0x10], EAX
c7 46 04 44 5f 26 01 MOV  [ESI+0x4], 0x01265f44
8b c6                MOV  EAX, ESI
5e                   POP  ESI
c3                   RET
```

The unique tail `c7 46 04 44 5f 26 01` (write the absolute VA
`0x01265f44` to `+4`) is a strong tell: grep `asm/ffxivgame/*.s` for
that 7-byte sequence to find any sibling that installs the same global
`DefaultMemoryAllocator` handle.

## Companion functions to look for

- **Base init** `FUN_00414de0` (3 B, `8b c1 c3` — `MOV EAX,ECX / RET`).
  Called here as an empty base ctor. Likely shared by several
  `Phieg::Base::*` subclass ctors.
- **Derived primary ctor**: somewhere a caller writes `this+0x00` with
  its own primary vftable after invoking FUN_00414d40 — that's the
  most-derived ctor in the chain. The most-derived class is what owns
  the allocator-handle field at `+0x4`.
- **Destructor pair**: a small `__thiscall` dtor that NULLs out
  `this+0x4` or just leaves it (allocator handles are typically not
  freed by the owner — they're singletons).
