# SQEX::CDev::Engine::Vfx::Common::Io::Printer

**Discovered**: 2026-05-24 by agent-4 while matching FUN_004158b0
**Owning RVA**: `Printer::SetLimit` @ 0x004158b0
**Source file (per debug strings)**: `.\\Io\\Printer.cpp` — lines 0xC0 (192) and 0xC3 (195)

## Fields (inferred from SetLimit's offsets)

Five "view slot" blocks share a uniform shape. Each is 20 bytes wide
(`{int head_pos, int tail_pos, void* data_ptr, void* meta_ptr, int chunks}`)
and carves out a stride*chunk_count window over the two top-level
buffers `m_data` (byte ring) and `m_meta` (8-byte-record ring).

| Offset | Type      | Field                                      |
|--------|-----------|--------------------------------------------|
| +0x08  | char*     | m_data           (cols * rows bytes)       |
| +0x0c  | void*     | m_meta           (rows * 8 bytes)          |
| +0x10  | int       | m_cols                                     |
| +0x14  | char*     | m_data_alias     (= m_data; view base)     |
| +0x18  | void*     | m_meta_alias     (= m_meta; view base)     |
| +0x1c  | int       | view0.head_pos   = 0                       |
| +0x20  | int       | view0.tail_pos   = 0                       |
| +0x24  | int       | view0.chunks     = chunks                  |
| +0x28  | char*     | view1.data       = m_data + chunks*cols    |
| +0x2c  | void*     | view1.meta       = m_meta + chunks*8       |
| +0x30  | int       | view1.head_pos   = 0                       |
| +0x34  | int       | view1.tail_pos   = 0                       |
| +0x38  | int       | view1.chunks     = chunks * 2              |
| +0x3c  | char*     | view2.data       = m_data + chunks*3*cols  |
| +0x40  | void*     | view2.meta       = m_meta + chunks*0x18    |
| +0x44  | int       | view2.head_pos   = 0                       |
| +0x48  | int       | view2.tail_pos   = 0                       |
| +0x4c  | int       | view2.chunks     = chunks * 3              |
| +0x50  | char*     | view3.data       = m_data + chunks*6*cols  |
| +0x54  | void*     | view3.meta       = m_meta + chunks*0x30    |
| +0x58  | int       | view3.head_pos   = 0                       |
| +0x5c  | int       | view3.tail_pos   = 0                       |
| +0x60  | int       | view3.chunks     = chunks * 4              |
| +0x64  | char*     | view4.data       = m_data + chunks*10*cols |
| +0x68  | void*     | view4.meta       = m_meta + chunks*0x50    |
| +0x6c  | int       | view4.head_pos   = 0                       |
| +0x70  | int       | view4.tail_pos   = 0                       |
| +0x74  | int       | view4.chunks     = chunks * 6              |

`chunks = rows / 16`, computed as `(rows + ((rows >> 31) & 0xf)) >> 4`.
A `rows < 16` (or `rows == 0`, or `cols == 0`) input short-circuits
the body and returns false without touching any field.

The 1/3/6/10-chunk-base layout suggests this is a **5-tier log/printer
ring** where each tier holds 2× the chunks of the prior (sizes 1, 2, 3,
4, 6 *chunks* per tier). The view at +0x60 has count `chunks * 4`, then
+0x74 has `chunks * 6` — the last tier holds 6 chunks but is positioned
10 chunks deep into the buffer, leaving 4 chunks at the head used by
tier-3. The total occupied size sums to 1+2+3+4+6 = 16 chunks, matching
the `chunks = rows/16` denominator. Each tier is presumably a different
verbosity/severity bucket (debug/info/warn/error/fatal?), with the size
ratios pre-baked into the class.

## Sibling methods

- `FUN_00415860` — `Printer::Reset()` — called as the first step of
  SetLimit (likely frees existing buffers and zeroes all view slots
  before SetLimit re-populates them).
- `FUN_00416480` — `SQEXAlloc(size, kind=9, align=0x10, callsite_fn,
  callsite_file, callsite_line)` — the engine's tagged-pool allocator.
  Used twice from SetLimit (alignment 0x10, kind 9 = "vfx" pool?).
- `FUN_009d2110` — CRT `memset` thunk.

## Calling convention

`__thiscall`, but with `ret 8` epilogue (the two stack args sized cleanly
into __stdcall-style cleanup). Returns `char` (1 success / 0 failure).
