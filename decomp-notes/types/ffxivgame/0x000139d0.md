# `SQEX::CDev::Engine::Memory::Alternative::DetachableHeapBlock` — ctor object layout

**Discovered**: 2026-05-24 by agent-4 while matching FUN_004139d0 (RVA 0x000139d0, 135 B).
**Vtables** (from `config/ffxivgame.rtti.json`):
- `0xb56f50` — DetachableHeapBlock primary (slot_count 3)
- `0xb56f18` — DetachableHeapBlock secondary, replaces IHandle base (slot_count 13)
- `0xb56f60` — DetachableHeapBlock embedded-Link override (slot_count 2)
- `0xb56750` — IHandle base vftable (initial write at +4, then overwritten)
- `0xb567c4` — Link vftable (initial write at +8 and at the two embedded
  sub-Link sentinels at +0x38 / +0x44; the +8 instance is overwritten by
  the DetachableHeapBlock vftable, the +0x38 / +0x44 ones stay as Link).

## Constructor signature

```
__thiscall DetachableHeapBlock *
DetachableHeapBlock::ctor(this,
                          param_1,  // [ESP+0x04] → stored at this+0x14
                          param_2,  // [ESP+0x08] → stored at this+0x18
                          param_3,  // [ESP+0x0c] → stored at this+0x1c
                          param_4,  // [ESP+0x10] → stored at this+0x28
                          param_5); // [ESP+0x14] → stored at this+0x2c
```

5 stack args, `RET 0x14`.

## Object layout (offsets touched by the ctor)

```
[this + 0x00]  primary vftable    DetachableHeapBlock::vftable (slot_count 3)
[this + 0x04]  secondary vftable  IHandle::vftable initially, then
                                  DetachableHeapBlock::vftable (slot_count 13)
[this + 0x08]  embedded Link      vftable=Link initially, then overwritten with
                                  DetachableHeapBlock::vftable (slot_count 2);
                                  next/prev (+0x0c, +0x10) self-init to &Link
[this + 0x14]  param_1 (DWORD)
[this + 0x18]  param_2 (DWORD)
[this + 0x1c]  param_3 (DWORD)
[this + 0x20]  0       (DWORD)
[this + 0x24]  0       (BYTE)
[this + 0x25]  0       (BYTE)
[this + 0x26]  0       (BYTE)
[this + 0x27]  0       (BYTE)
[this + 0x28]  param_4 (DWORD)
[this + 0x2c]  param_5 (DWORD)
[this + 0x30]  0       (DWORD)
[this + 0x34]  0       (DWORD)
[this + 0x38]  embedded Link sentinel — vftable=Link, next=prev=&self
[this + 0x44]  embedded Link sentinel — vftable=Link, next=prev=&self
[this + 0x50]  ... end of touched range
```

Minimum total instance size: 0x50 (80 B) for the fields written here, but the
two trailing Link sub-sentinels may be backed by further members; siblings
that walk these lists will reveal the actual end-of-object offset.

## Identifying signature

The MI-style constructor shape recurs across the
`SQEX::CDev::Engine::Memory::Alternative::*` family. The fingerprint:

```
MOV EDX, [ESP+0x8]
MOV EAX, ECX                       ; EAX = this
MOV [EAX+0x4], <imm32_base_vftable>
MOV [EAX+0x8], <imm32_link_vftable>
LEA ECX, [EAX+0x8]                 ; ECX = &embedded Link
MOV [ECX+0x4], ECX                 ; Link.next = self
MOV [ECX+0x8], ECX                 ; Link.prev = self
MOV [ECX],   <imm32_derived_vftable>   ; overwrite the Link vftable
...
MOV [EAX],   <imm32_derived_primary_vftable>      ; primary at +0
MOV [EAX+0x4], <imm32_derived_secondary_vftable>  ; overwrite the base vftable at +4
```

i.e. base subobject vftables are written first (because that's the order
the *bases* would do it if they were called as ctors), then the most-derived
class's vftable slots are written last (this is the dispatch behaviour
that lets a partially-constructed object call its own least-derived
virtual functions while the more-derived ctor body runs).

## How to find sibling ctors

Grep the asm dumps for `MOV [EAX+0x4], imm32` where the imm32 matches an
IHandle / Link / SeparateHeapBlock / DetachableHeapBlock / RemovableHeapBlock
vftable RVA (see `config/ffxivgame.rtti.json` near `0xb567..0xb56f`). The
trailing `RET 0x14` (or 0x10 / 0x18 depending on arg count) plus the
`88 48 24` / `88 48 25` / `88 48 26` / `88 48 27` four-byte-zero burst at
the same byte cluster is a strong fingerprint for this family's ctor.
