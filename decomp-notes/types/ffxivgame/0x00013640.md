# `SQEX::CDev::Engine::Memory::Alternative::DetachableHeapSpace`

**Discovered**: 2026-05-24 while matching FUN_00413640 (88 B, __thiscall dtor)
**RTTI hint**: Ghidra headless decomp tagged five sibling vftables in this
namespace by symbol — `DetachableHeapSpace::vftable` (0x00f56ed4),
`IDebugBlock::vftable` (0x00f56788), `IDebugSpace::vftable` (0x00f567b4),
`Link::vftable` (0x00f567c4), `ISpace::vftable` (0x00f566fc).

## Hierarchy (inferred from the dtor's vftable reseat sequence)

The destructor at 0x00413640 reseats the primary vftable slot four times
on its way out — the canonical MSVC pattern for "most-derived → base"
walking through the inheritance chain so that any vcall during a sub-dtor
sees the correct typeid. The reseat order is:

```
DetachableHeapSpace  ← entry (most-derived)
  ↓ ... (this dtor's body — no base sub-dtor CALL is emitted; the bases
  ↓     are either trivial or fully inlined)
ISpace               ← exit (final reseat just before POP/RET)
```

Combined with the two embedded subobject vftable reseats (`[this+0x54]`
→ `IDebugSpace::vft` and `[this+0x5C]` → `IDebugBlock::vft`), the
inheritance chain is most likely:

```
class DetachableHeapSpace
  : public ISpace          // primary base, vftable at [this+0x00]
  , public IDebugSpace     // secondary base, vftable at [this+0x54]
  , public IDebugBlock     // tertiary base,  vftable at [this+0x5C]
{
    // members...
};
```

The vftable layout in the orig binary (5 distinct vftables in adjacent
24-byte stride: 0x00f566fc, 0x00f56788, 0x00f567b4, 0x00f567c4, 0x00f56ed4)
is consistent with a typical MSVC RTTI / vftable cluster for one class
with two secondary bases.

## Member layout (from dtor body — byte offsets from `this`)

| Offset | Size | Type                          | Notes                                                            |
|--------|------|-------------------------------|------------------------------------------------------------------|
| +0x00  | 4    | vftable*                      | primary (ISpace / IDebugSpace / IDebugBlock / DetachableHeapSpace) |
| +0x24  | 4    | Link::vftable*                | embedded `Link` subobject #2 — vftable slot                      |
| +0x28  | 4    | Link*                         | link1.next  (intrusive doubly-linked list)                       |
| +0x2C  | 4    | Link*                         | link1.prev                                                       |
| +0x30  | 4    | Link::vftable*                | embedded `Link` subobject #1 — vftable slot                      |
| +0x34  | 4    | Link*                         | link0.next                                                       |
| +0x38  | 4    | Link*                         | link0.prev                                                       |
| +0x3C  | 24   | CRITICAL_SECTION              | Win32 critsec (`+0x3C..+0x53`, deleted via `DeleteCriticalSection`) |
| +0x54  | 4    | IDebugSpace::vftable*         | embedded secondary base vftable slot                             |
| +0x58  | 4    | (subobject body or padding)   | (not touched by this dtor)                                       |
| +0x5C  | 4    | IDebugBlock::vftable*         | embedded tertiary base vftable slot                              |
| +0x60+ | ?    | (subobject body or padding)   | (not touched by this dtor)                                       |

Total object size lower bound: 0x60 (96 B). The dtor doesn't fix an upper
bound, but the IDebugBlock embedded subobject at +0x5C likely extends at
least 4 more bytes (its vftable plus any inline fields), so 0x60–0x80 is
the most-plausible range.

## Subobject: `Link` — intrusive doubly-linked list node

`Link` has at least three fields (vftable + next + prev, the canonical
8-byte intrusive node layout). The dtor unlinks two embedded `Link`
subobjects from their parent lists:

```cpp
// link0 unlink:
this->link0.next->prev = this->link0.prev;
this->link0.prev->next = this->link0.next;
// link1 unlink (same shape):
this->link1.next->prev = this->link1.prev;
this->link1.next->prev = this->link1.prev;   // (sic — see below)
```

`Link` member layout (from the `[ECX+8] = ...` and `[ECX+4] = ...`
stores):

| Offset | Size | Field         |
|--------|------|---------------|
| +0x00  | 4    | vftable*      |
| +0x04  | 4    | Link* next    |
| +0x08  | 4    | Link* prev    |

(or possibly `next` at +0x08 and `prev` at +0x04 — the orig stores the
"prev" pointer to `[link->next + 8]` and "next" to `[link->prev + 4]`,
which is the standard `node->prev->next = …` shape, putting `next` at
+0x04 and `prev` at +0x08.)

## Why two embedded `Link` subobjects?

Two distinct list memberships — likely a "global heap-space registry"
list (link0) and a "debug / leak tracker" list (link1) given the
`DebugSpace` / `DebugBlock` base classes. The order of unlink matters
for crash-safety: the inner-most subsystem (debug, link1) is unlinked
after the outer-most (registry, link0), which mirrors the
reverse-construction order MSVC enforces for subobject destruction.

## Sibling functions to look for (constructor / clone / init)

The constructor (matching this dtor) should be the most informative
sibling. Look for a `__thiscall` ctor near the same RVA cluster that:

- Writes the same 5 vftable addresses to the same 5 offsets
- Calls `InitializeCriticalSection(&this->crit)` for the +0x3C member
- Inserts the two `Link` subobjects into their parent lists (canonical
  `node->prev = head->prev; node->next = head; head->prev->next = node;
  head->prev = node;` sequence — easy to grep for)

If found, it'll confirm both the offsets above AND give us the parent
list-head globals' RVAs, which are otherwise un-discoverable from the
dtor alone.
