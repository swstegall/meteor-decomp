# SQEX::CDev::Engine::Vfx::Common::Io::Printer — ctor + vftable

**Discovered**: 2026-05-24 by agent-1 while matching FUN_00415bf0
**Owning RVA**: `Printer::Printer(int cols, int rows)` @ 0x00015bf0

Complements the class layout in
`decomp-notes/types/ffxivgame/0x000158b0.md` (Printer::SetLimit).

## Vftable

| Offset  | Address       | Notes                                    |
|---------|---------------|------------------------------------------|
| +0x00   | 0x00f5763c    | Printer vftable pointer (.rdata)         |

## Constructor sequence (matches asm at 0x00015bf0)

```
Printer::Printer(int cols, int rows) {
    this->m_flag_4    = 1;          // [+0x04] byte — "owns m_data" flag
    this->m_flag_5    = 1;          // [+0x05] byte — "owns m_meta" flag
    this->m_data      = NULL;       // [+0x08] — primary buffer pointer
    this->m_meta      = NULL;       // [+0x0c] — secondary buffer pointer
    this->m_cols      = 0;          // [+0x10] int
    this->m_field_78  = 0;          // [+0x78] int — purpose unknown
    this->m_byte_81   = 0;          // [+0x81]
    this->m_subobj    = NULL;       // [+0x7c] — released via FUN_00416650
    this->m_byte_91   = 0;          // [+0x91]
    this->m_byte_80   = 0;          // [+0x80]
    this->vftable     = &Printer::vftable;  // [+0x00] = 0x00f5763c
    SetLimit(cols, rows);           // FUN_004158b0 — populates view tiers
}
```

## Additional field discoveries (vs the SetLimit type doc)

The ctor touches three byte slots that SetLimit doesn't:

| Offset  | Type | Initial value | Purpose (best guess)           |
|---------|------|---------------|--------------------------------|
| +0x04   | byte | 1             | owns-m_data flag               |
| +0x05   | byte | 1             | owns-m_meta flag               |
| +0x78   | int  | 0             | unknown — paired with +0x7c    |
| +0x80   | byte | 0             | unknown — verbose / debug flag |
| +0x81   | byte | 0             | unknown — verbose / debug flag |
| +0x91   | byte | 0             | unknown — verbose / debug flag |

The two `owns` flags at +0x04/+0x05 govern whether `Printer::Reset`
(FUN_00415860) frees `m_data` / `m_meta` — Reset only frees the pair
when `m_data` is non-null (after `SetLimit` allocated them).

## Calling convention

`__thiscall` with `ret 8` — same as the rest of the class (SetLimit
takes two stack args + `this` in ECX and pops the args; the ctor
forwards exactly that shape).
